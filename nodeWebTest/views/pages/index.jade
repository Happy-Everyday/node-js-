extends ../layout
block content
    .container
        .row
            .col-md-2
                each item in movies
                    .thumbnail
                        a(href='/movie')
                            img(src="#{item.poser}", alt="#{item.title}")
                        .caption
                            h3 #{title}
                            p: a.btn.btn-primary(href='#{item.poser}', role='button') 观看预告片